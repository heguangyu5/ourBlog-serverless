<?php
    $this->headTitle('后台首页');
?>

<div class="text-lft">
<?php
    $rows = Zend_Db_Table_Abstract::getDefaultAdapter()->fetchAll(
        "SELECT id, title FROM posts WHERE uid = ? ORDER BY id DESC",
        $this->uid,
        Zend_Db::FETCH_OBJ
    );
    foreach ($rows as $row):
?>
    <div class="mar-btm">
        <?php echo htmlspecialchars($row->title); ?>
        <div class="pull-right">
            <a href="/admin/edit/?id=<?php echo $row->id; ?>">编辑</a>
            <a href="/admin/delete/?id=<?php echo $row->id; ?>">删除</a>
        </div>
    </div>
<?php endforeach; ?>
</div>
